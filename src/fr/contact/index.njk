---
layout: base.njk
lang: fr
title: "Contact | Anjo Cleaning MadÃ¨re"
description: "Contactez Anjo Cleaning MadÃ¨re pour turnover AL, rÃ©sidences privÃ©es et locaux commerciaux avec processus clairs, qualitÃ© fiable et rÃ©ponse rapide."
permalink: /fr/contact/

langLinks:
  de: /de/fr/contact/
  en: /en/fr/contact/
  fr: /fr/fr/contact/
  pt: /pt/fr/contact/
---

<style>
/* Premium Contact â€“ scoped */
/* CONTACT_LOCK: keep contact form identical to live. */
.contact-premium{
  max-width: 980px;
  margin: 0 auto;
  padding: 6px 0 18px;
}

.contact-premium .subline{
  max-width: 760px;
  margin: 8px 0 18px;
  opacity: .9;
}

.contact-premium[data-contact-lock="live"] .card{
  background: rgba(255,255,255,.08);
  border-radius: 18px;
  padding: 18px;
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  box-shadow: 0 10px 30px rgba(0,0,0,.08);
}

.contact-premium .grid{
  display: grid;
  grid-template-columns: 1fr;
  gap: 14px;
}

@media (min-width: 760px){
  .contact-premium .grid.two{
    grid-template-columns: 1fr 1fr;
    gap: 14px 18px;
  }
}

.contact-premium label{
  display: block;
  font-weight: 700;
  margin: 0 0 8px;
}

.contact-premium .req{
  opacity: .7;
  font-weight: 700;
}

.contact-premium[data-contact-lock="live"] .field{
  width: 100%;
  border: 0;
  outline: none;
  border-radius: 16px;
  padding: 14px 14px;
  background: rgba(0,0,0,.14);
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.08);
  color: inherit;
  transition: box-shadow .18s ease, background .18s ease;
}

.contact-premium .field::placeholder{
  opacity: .7;
}

.contact-premium .field:focus{
  background: rgba(0,0,0,.10);
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,.14),
    0 0 0 4px rgba(0,31,63,.18);
}

.contact-premium textarea.field{
  min-height: 170px;
  resize: vertical;
  line-height: 1.35;
}

/* Custom dropdown */
.contact-premium .select{
  position: relative;
}

.contact-premium .selectBtn{
  width: 100%;
  text-align: left;
  border: 0;
  outline: none;
  border-radius: 16px;
  padding: 14px 44px 14px 14px;
  background: rgba(0,0,0,.14);
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.08);
  color: inherit;
  cursor: pointer;
  transition: box-shadow .18s ease, background .18s ease;
}

.contact-premium .selectBtn:focus{
  background: rgba(0,0,0,.10);
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,.14),
    0 0 0 4px rgba(0,31,63,.18);
}

.contact-premium .selectBtn .placeholder{
  opacity: .7;
}

.contact-premium .chev{
  position: absolute;
  right: 14px;
  top: 50%;
  transform: translateY(-50%);
  width: 18px;
  height: 18px;
  opacity: .9;
  pointer-events: none;
}

.contact-premium .listbox{
  position: absolute;
  left: 0;
  right: 0;
  top: calc(100% + 8px);
  z-index: 20;
  display: none;
  border-radius: 16px;
  background: rgba(0,0,0,.14);
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,.08),
    0 14px 34px rgba(0,0,0,.18);
  overflow: hidden;
}

.contact-premium .listbox.open{
  display: block;
}

.contact-premium .option{
  width: 100%;
  border: 0;
  background: transparent;
  color: inherit;
  text-align: left;
  padding: 12px 14px;
  cursor: pointer;
  transition: background .14s ease;
}

.option--spacer{
  pointer-events:none;
  cursor: default;
  opacity: 0;
}

.contact-premium .contact-premium .option:hover,
.contact-premium .option[aria-selected="true"]{
  background: rgba(0,0,0,.10);
}

.contact-premium .checkwrap{
  margin-top: 12px;
  background: rgba(0,0,0,.14);
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.08);
  border-radius: 16px;
  padding: 14px 14px;
  display: flex;
  gap: 12px;
  align-items: flex-start;
}

.contact-premium input[type="checkbox"]{
  width: 18px;
  height: 18px;
  margin-top: 2px;
  accent-color: #001f3f;
  flex: 0 0 auto;
}

.contact-premium .fineprint{
  margin-top: 12px;
  font-size: .95rem;
  opacity: .92;
}

.contact-premium .mutedLink{
  color: inherit;
  opacity: .95;
  text-decoration: underline;
  text-underline-offset: 3px;
}

.contact-premium .actions{
  margin-top: 14px;
  display: flex;
  gap: 10px;
  align-items: center;
}

.contact-premium .btnPremium{
  background: none;
  border: none;
  padding: 0;
  font-weight: 800;
  font-size: 1rem;
  color: #001f3f;
  cursor: pointer;
  transition: opacity .2s ease;
}

.contact-premium .btnPremium:hover{
  opacity: .7;
}

.contact-premium .btnPremium:disabled{
  opacity: .55;
  cursor: not-allowed;
}

.contact-premium .hintline{
  font-size: .95rem;
  opacity: .85;
}

.contact-premium .status{
  margin-top: 12px;
  border-radius: 16px;
  padding: 12px 14px;
  display: none;
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.10);
}

.contact-premium .status.ok{
  background: rgba(46, 204, 113, .12);
}

.contact-premium .status.err{
  background: rgba(231, 76, 60, .12);
}
</style>


  <div class="hero-logo hero-logo--small">
    <img src="/assets/images/logo.svg" alt="Anjo Cleaning Logo" width="720" height="220" loading="eager" decoding="async">
  </div>
<div class="contact-premium" data-contact-lock="live">
  <div class="topRow">
    <h1 id="title">Contact</h1>
  </div>

  <p class="subline" id="intro">
    Ã‰crivez-nous pour la disponibilitÃ©, les tarifs ou un plan de nettoyage sur mesure. Nous rÃ©pondons le plus rapidement possible.
  </p>

  <div class="card">
    <form action="/send.php" id="contactForm" method="post" novalidate="">
      <!-- Honeypot -->
      <div aria-hidden="true" style="position:absolute; left:-10000px; top:auto; width:1px; height:1px; overflow:hidden;">
        <label for="website">Website</label>
        <input id="website" name="website" type="text" tabindex="-1" autocomplete="off"/>
      </div>

      <input type="hidden" name="lang" value="fr"/>
      <input type="hidden" name="page" value="/fr/contact/"/>

      <div class="grid two">
        <div>
          <label for="name">Nom <span class="req">*</span></label>
          <input class="field" id="name" name="name" type="text" autocomplete="name" placeholder="Votre nom" required=""/>
        </div>

        <div>
          <label for="email">E-mail <span class="req">*</span></label>
          <input class="field" id="email" name="email" type="email" autocomplete="email" placeholder="info@anjo-cleaning.com" required=""/>
        </div>
      </div>

      <div class="grid two" style="margin-top:12px;">
        <div>
          <label for="phone">TÃ©lÃ©phone (optionnel)</label>
          <input class="field" id="phone" name="phone" type="tel" autocomplete="tel" placeholder="+351 â€¦"/>
        </div>

        <div>
          <label for="serviceSelectBtn">Service <span class="req">*</span></label>

          <input type="hidden" id="service" name="service" value="" required=""/>

          <div class="select" data-select>
            <button
              type="button"
              class="selectBtn"
              id="serviceSelectBtn"
              aria-haspopup="listbox"
              aria-expanded="false"
            >
              <span class="value"><span class="placeholder">SÃ©lectionnerâ€¦</span></span>
              <svg class="chev" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M7 10l5 5 5-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>

            <div class="listbox" role="listbox" aria-label="Choisir un service">
              <button type="button" class="option option--spacer" aria-hidden="true" tabindex="-1"></button>
              <button type="button" class="option" role="option" data-value="house_sitting" aria-selected="false">Property Care Luxe</button>
              <button type="button" class="option" role="option" data-value="services_luxury" aria-selected="false">Services Luxe</button>
              <button type="button" class="option" role="option" data-value="storm_check" aria-selected="false">ContrÃ´le tempÃªte (extÃ©rieur)</button>
              <button type="button" class="option" role="option" data-value="holiday_al" aria-selected="false">AL Alojamento Local</button>
              <button type="button" class="option" role="option" data-value="other" aria-selected="false">Autre</button>
            </div>
          </div>
        </div>
      </div>

      <div style="margin-top:12px;">
        <label for="message">Message <span class="req">*</span></label>
        <textarea class="field" id="message" name="message" placeholder="De quoi sâ€™agit-il ? (lieu, taille, dates, demandes particuliÃ¨res)â€¦" required=""></textarea>
      </div>

      <div class="checkwrap">
        <input id="consent" name="consent" type="checkbox" required=""/>
        <div class="fineprint">
          Jâ€™ai lu et pris connaissance de la <a class="mutedLink" href="/fr/privacy/">politique de confidentialitÃ©</a>. <span class="req">*</span>
        </div>
      </div>

      <div class="fineprint" id="retentionText">
        <b>Conservation :</b> demandes sans contrat jusquâ€™Ã  12 mois ; documents contractuels/comptables gÃ©nÃ©ralement 10 ans (Portugal).
      </div>

      <div class="fineprint" id="processingText">
        <b>Traitement :</b> nous utilisons vos donnÃ©es pour rÃ©pondre Ã  votre demande et pour des mesures prÃ©contractuelles (Art. 6(1)(b) RGPD).
        <b>Destinataires :</b> aucune transmission Ã  des tiers ; envoi via e-mail serveur.
        <b>Droits :</b> accÃ¨s/effacement/opposition, etc. voir la <a class="mutedLink" href="/fr/privacy/">politique de confidentialitÃ©</a>.
      </div>

      <div class="actions">
        <button class="btnPremium" id="btnSend" type="submit">Envoyer</button>
        <div class="hintline" id="hint">RÃ©ponse gÃ©nÃ©ralement sous 24 h (jours ouvrÃ©s).</div>
      </div>

      <div class="status err" id="errBox" role="status" aria-live="polite"></div>
      <div class="status ok" id="okBox" role="status" aria-live="polite"></div>
    </form>
  </div>

<script>
(function(){
  const form = document.getElementById("contactForm");
  const err = document.getElementById("errBox");
  const ok  = document.getElementById("okBox");
  const btn = document.getElementById("btnSend");

  const hiddenService = document.getElementById("service");
  const selectWrap = document.querySelector("[data-select]");
  const selectBtn = document.getElementById("serviceSelectBtn");
  const listbox = selectWrap ? selectWrap.querySelector(".listbox") : null;
  const valueEl = selectWrap ? selectWrap.querySelector(".value") : null;
  const options = listbox ? Array.from(listbox.querySelectorAll(".option[data-value]")) : [];



  // Restrict service options when coming from Property Care / Locations pages
  try{
    const qs = new URLSearchParams(window.location.search);
    const src = qs.get("src") || sessionStorage.getItem("src");
    if(src === "orte"){
      const allowed = new Set(["storm_check","house_sitting"]);
      options.forEach(opt=>{
        const v = opt.getAttribute("data-value");
        if(!allowed.has(v)){
          opt.style.display = "none";
        }
      });
      // If a hidden option was preselected, reset selection
      if(hiddenService && hiddenService.value && !allowed.has(hiddenService.value)){
        hiddenService.value = "";
        if(valueEl) valueEl.textContent = "";
      }
      // Clear the marker after applying
      if(!qs.get("src")){ sessionStorage.removeItem("src"); }
    }
  }catch(e){}

  if(!form || !err || !ok || !btn) return;

  function escapeHtml(s){
    return String(s).replace(/[&<>"']/g, (c)=>({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#39;" }[c]));
  }

  function setBusy(isBusy){
    btn.disabled = isBusy;
    btn.setAttribute("aria-busy", isBusy ? "true" : "false");
    if(isBusy){
      btn.dataset.label = btn.textContent;
      btn.textContent = "Envoiâ€¦";
    }else{
      btn.textContent = btn.dataset.label || "Envoyer";
    }
  }

  function showErr(msg){
    err.innerHTML = "<b>Erreur :</b> " + escapeHtml(msg);
    err.style.display = "block";
    ok.style.display = "none";
  }

  function showOk(msg){
    ok.innerHTML = "<b>EnvoyÃ© :</b> " + escapeHtml(msg);
    ok.style.display = "block";
    err.style.display = "none";
  }

  function val(id){
    return (document.getElementById(id)?.value || "").trim();
  }

  function openList(){
    if(!listbox) return;
    listbox.classList.add("open");
    selectBtn.setAttribute("aria-expanded", "true");
  }

  function closeList(){
    if(!listbox) return;
    listbox.classList.remove("open");
    selectBtn.setAttribute("aria-expanded", "false");
  }

  function setSelected(value, label){
    if(hiddenService){
      hiddenService.value = value || "";
      hiddenService.setAttribute("value", value || "");
    }
    if(valueEl){
      valueEl.innerHTML = label ? escapeHtml(label) : '<span class="placeholder">SÃ©lectionnerâ€¦</span>';
    }
    options.forEach(o=>{
      o.setAttribute("aria-selected", o.dataset.value === value ? "true" : "false");
    });
  }


  // Optional: restrict service options when coming from most location pages
  const params = new URLSearchParams(window.location.search || "");
  const from = (params.get("from") || "").trim();
  const serviceParam = (params.get("service") || "").trim();
  const locationExceptions = ["ponta-do-sol","calheta","ribeira-brava"];

  if(from && !locationExceptions.includes(from)){
    options.forEach(o=>{
      const v = (o.dataset.value || "");
      if(v !== "house_sitting" && v !== "storm_check"){
        o.style.display = "none";
      }else{
        o.style.display = "";
      }
    });
  }

  // Optional: preselect service from URL (?service=house_sitting | storm_check)
  if(serviceParam && options.length){
    const opt = options.find(o => (o.dataset.value || "") === serviceParam);
    if(opt){
      setSelected(serviceParam, opt.textContent || "");
    }
  }

  if(selectBtn && listbox){
    selectBtn.addEventListener("click", ()=>{
      if(listbox.classList.contains("open")) closeList();
      else openList();
    });

    options.forEach(o=>{
      o.addEventListener("click", ()=>{
        setSelected(o.dataset.value || "", o.textContent || "");
        closeList();
      });
    });

    document.addEventListener("click", (e)=>{
      if(!selectWrap) return;
      if(selectWrap.contains(e.target)) return;
      closeList();
    });

    document.addEventListener("keydown", (e)=>{
      if(e.key === "Escape") closeList();
    });
  }

  form.addEventListener("submit", async (e)=>{
    e.preventDefault();

    const name = val("name");
    const email = val("email");
    const service = hiddenService ? (hiddenService.value || "") : "";
    const message = val("message");
    const consent = !!document.getElementById("consent")?.checked;

    if(!name || !email || !service || !message){
      return showErr("Veuillez remplir tous les champs obligatoires (*).");
    }
    if(!consent){
      return showErr("Veuillez confirmer la politique de confidentialitÃ© (case Ã  cocher).");
    }

    setBusy(true);

    try{
      const fd = new FormData(form);
      fd.set("lang", "fr");

      const res = await fetch(form.action, { method: "POST", body: fd });
      const text = await res.text();

      if(res.status === 429){
        let retry = 0;
        try{
          const data429 = JSON.parse(text);
          retry = parseInt(data429.retry_after || 0, 10);
        }catch(_e){}
        if(retry > 0) throw new Error(("Trop de demandes. Veuillez patienter {s} secondes.").replace("{s}", String(retry)));
        throw new Error("Trop de demandes. Veuillez patienter un instant et rÃ©essayer.");
      }

      let data = null;
      try{ data = JSON.parse(text); }catch(_e){ data = null; }

      if(data){
        if(data.error) throw new Error(data.error);
        if(!(res.ok && data.ok !== false)) throw new Error(data.message || "Erreur serveur");
      }else{
        if(!res.ok) throw new Error("Erreur serveur");
      }

      showOk("Merci. Votre demande a Ã©tÃ© envoyÃ©e.");
      form.reset();
      setSelected("", "");
      closeList();
    }catch(ex){
      showErr("Ã‰chec de lâ€™envoi : " + (ex && ex.message ? ex.message : "Erreur inconnue"));
    }finally{
      setBusy(false);
    }
  });
})();
</script>

